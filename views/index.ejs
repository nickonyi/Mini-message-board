<!DOCTYPE html>
<html>
  <head>
    <title>Message Board</title>
  </head>
  <body>
    <ul>
      <% messages.forEach((message) => { %> <%- include('message', { message:
      message }) %> <% }) %>
    </ul>
  </body>
</html>
